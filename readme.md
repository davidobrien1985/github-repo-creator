# Manage GitHub repositories via an API endpoint

* create repositories
  * configure repositories
    * create default branches
    * configure merge strategy
    * configure protected branches
    * configure admins
